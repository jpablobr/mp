#!/bin/bash

# the basics
: ${HOME=~}
: ${LOGNAME=$(id -un)}
: ${UNAME=$(uname)}

# complete hostnames from this file
: ${HOSTFILE=$HOME/.ssh/known_hosts}
: ${HOSTNAME=$(/bin/hostname)}
# readline config
: ${INPUTRC=~/.inputrc}

#-----------------------------------------------------------------------
# - Plugins
PLUGINS=(
    "env"
    "options"
    "sourcer"
    "pager-editor"
    "ls-colors"
    "completions"
    "functions"
    "path"
    "aliases"
    "ssh"
    "prompt" )

for plugin in "${PLUGINS[@]}"
do
    plugin_path="$HOME/.my-precious/bash/plugins/${plugin}"
    test -f $plugin_path && source $plugin_path
done